# Лабораторная работа 2
## Тема: Структуры в языке C
## Цель работы: изучить объявление и использование структур, вложенные структуры, указатели на структуры, массивы структур, передачу структур в функции, перечисления (enum) и объединения (union).

---

### Задание 1. Базовая структура
**Структура Student:**
- имя
- возраст
- средний балл

**Задачи:**
1. Объявить переменную структуры
2. Заполнить ее значениями
3. Вывести данные на экран
4. Организовать ввод данных с клавиатуры

### Задание 2. Массив структур
**Задачи:**
1. Создать массив из 5 структур Student
2. Заполнить массив данными
3. Вывести всех студентов
4. Найти студента с максимальным средним баллом

### Задание 3. Вложенные структуры
**Структура Date:**
- день
- месяц
- год

**Структура Person:**
- имя
- дата рождения (структура Date)

**Задачи:**
1. Заполнить структуру
2. Вывести данные
3. Вывести только тех, кто родился после 2000 года

### Задание 4. Структуры и функции
**Структура Rectangle:**
- ширина
- высота

**Задачи:**
1. Написать функцию вычисления площади
2. Написать функцию вычисления периметра
3. Передавать структуру в функции:
   - по значению
   - по указателю

### Задание 5. Перечисления (enum)
**Задачи:**
1. Создать перечисление Day, содержащее дни недели
2. Создать переменную типа enum
3. Вывести значение с помощью конструкции switch
4. Использовать enum внутри структуры

### Задание 6. Объединения (union)
**Объединение Data:**
- int
- float
- char

**Задачи:**
1. Присвоить значения разным полям
2. Показать, что хранится только одно значение
3. Создать структуру, содержащую union и поле, указывающее тип хранимого значения

### Задание 7. Комплексная работа со структурами
**Структура Student:**
- имя
- возраст
- курс (использовать enum)
- средний балл

**Задачи:**
1. Создать массив студентов
2. Реализовать функции:
   - добавление студента
   - вывод списка студентов
   - поиск по имени
   - сортировка по среднему баллу
3. Использовать указатели и разнести программу на функции

---

## Вопросы:
1. Как объявить структуру в языке C
2. Что такое вложенные структуры и зачем они нужны
3. Как передать структуру в функцию
4. Можно ли создавать массивы структур
5. Как обратиться к элементу массива структур
6. Можно ли использовать enum внутри структуры
7. Можно ли сравнивать структуры оператором ==

---

## Краткая теория

**Структура в языке C** — это пользовательский тип данных, объединяющий переменные разных типов под одним именем.

Для работы со структурами используется ключевое слово struct.

**Объявление структуры**

struct Student {
    char name[50];
    int age;
    float gpa;
};
##Ответы на вопросы: 
1)
struct Имя { тип; };
typedef struct Имя { тип; } Псевдоним;
2)
Структура внутри структуры. Нужны для группировки данных.
struct Data{ int day; };
struct People{ struct when_he_bern; };
3)
void Skud(struct Student s) {
    printf("%s", s.name);} - копирует
 vid Skud(struct Student *s) {
    s->age = 21; - меняет
}
4)
struct S arr[10];
5)
arr[i].поле — точка для массива
arr[i]->поле — стрелка для указателя
6)
typedef struct Student {
    char name[20];
    int age;
    enum{First = 1, Second, Third, Fourth} course;
    float med_ball;
} Stud;
7)
нет можно с помощью strcmp 
if (strcmp(str, S[i].name) == 0) printf("%d Student\t%s\t%d\t%d\t%g\n", i+1, S[i].name, S[i].age, S[i].course, S[i].med_ball);
